# Space Greenhouse 開發指引

本文件供在此 repository 工作的 coding agent 使用，適用於整個專案。這裡的 coding agent 是開發協作者；遊戲內的 Core／Plant／Human Agent 是產品角色，兩者不要混淆。

## 1. 專案目標與本次分工

Space Greenhouse 是以火星基地為場景的 AI 自主生存經營展示：在有限水、氧氣、食物與電力下，由 AI 權衡人類與作物需求，持續維持基地運作。展示重點是「觀察 → 建議 → 決策 → 執行 → 結果 → 重新規劃」的可見過程。

使用者負責**前端與後端設計**。預設工作範圍：

- 前端地圖、世界物件、角色動畫、互動、Agent 訊息與狀態呈現。
- 後端世界模型、tick 結算、計畫驗證、控制 API、狀態推播與紀錄。
- 與隊友負責的 Core／Plant／Human Agent 串接所需的 schema、adapter、驗證錯誤及 mock fixtures。

使用者最新澄清僅負責前後端整合。不得實作遊戲內 Core／Plant／Human 決策、LLM provider、提示詞或記憶系統；這些由隊友提供。只建立 TickPlan／Agent 訊息串接接口與明確標示的展示 mock。開發用 multi-agent 分工不等於實作遊戲內 Agents。Phase 4 調整為串接與 mock 驗證。

## 2. 文件依據與優先順序

開始工作前，閱讀與任務相關的原始文件：

1. 使用者最新明確指示。
2. [world-settings-guide.md](world-settings-guide.md)：世界數值、公式、結算順序、工作占用與死亡規則的主要依據。
3. [space_greenhouse_spec_v2.md](space_greenhouse_spec_v2.md)：前後端架構、視覺、資料模型、通訊與驗收的設計基準；與 guide 衝突時以 guide 為準。
4. [OpenAI Hackathon.pdf](OpenAI%20Hackathon.pdf)：專案動機與 Agentic AI 概念，不能以早期構想覆蓋新版世界規則。
5. 本文件：日常實作與協作指引，不是另一份世界參數表。

目前 repository 只有上述三份設計文件，尚無應用程式或測試骨架。v2 所述「既有程式碼」「前端已完成」是文件中的描述，不代表本 checkout 存在那些檔案。guide 引用的 `world-rules-spec.md` 與 `checks/*.py` 目前也不存在；不得聲稱已讀取或已執行。

遇到衝突，回報來源、具體差異與採用依據。已有 guide 規則可解決的差異直接依 guide 處理；會改變遊戲語意且尚無依據的決定，標記 `TODO(guide-pending)`，繼續其他不受影響的工作。

## 3. 不可破壞的責任邊界

- **Core 決策、Plant／Human 提建議、後端驗證與結算、前端呈現。** 建議和自然語言訊息不能直接修改世界。
- **世界狀態由後端權威管理。** tick 結算只有一個入口；玩家改資源與 reset 也須經後端驗證和一致的狀態提交流程。
- **數值只有一份。** 將 guide 的參數集中為共用設定，前端與 Agent adapter 讀取同一來源或其生成產物。不要在元件、模型與 prompt 分別硬編。
- 後端固定的是世界規則，不是生存策略。不得自動替 Core 選擇補充對象、灌溉優先序、作物配置或緊急保留量。
- guide 明定的發電縮量比例分配是世界公式，必須保留；它不等於可對其他資源自行採平均分配。
- 警戒線用於提示風險，不是拒絕操作的安全底線。合法但會致死的計畫，不能被驗證器偷偷改成保命策略。
- 前端動畫完成、WebSocket 重送與 Agent 訊息重播，都不能造成第二次扣款或採收。

## 4. 世界規則實作底線

完整數值以 guide 為準；下列是容易實作錯誤的限制：

- 四種公共資源為 `water`（L）、`oxygen`（OU）、`food`（遊戲 kcal）、`power`（EU）。個人 `food_energy` 和 `water` 與公共庫存分開。
- 4 名 crew、20 塊地、每塊 5 m²，初始五種作物各四塊。沒有 health、職業加成、種子費、可調光照或可調溫度。
- 1 tick 為 1 模擬小時，預設每 2 秒推進。只有作物採文獻一天映射成一個有效生長 tick；人類需求仍按每日數值除以 24。
- 計算保留精度，僅在 UI 格式化時四捨五入。表格中的近似小數不能作為逐 tick 消耗常數。
- 進食／飲水是公共庫存到個人存量的 1：1 轉移，各占一個工作 tick；初始個人存量不再從公共庫存扣一次。
- 同人同 tick 只能做一项占用工作：發電、進食、飲水、播種、採收或清除。部分發電仍占用該 tick。
- 發電使用個人能量與公共氧氣，不再次扣公共食物；目前係數是每工作單位 100 遊戲 kcal + 25 OU → 250 EU。
- 存活地塊每 tick 需完整 8.7 L + 5 EU，兩項原子扣除。缺任一項或未獲完整分配時兩項都不扣，連續未灌溉次數加一；第 3 次當 tick 死亡。
- 成熟作物仍需灌溉並持續產氧，但不繼續累積收成。空地與死亡作物不耗水電、不產氧。
- 採收須容納完整收成才成功；成功後清空地塊。播種後下一 tick 才生長。同地塊多項操作依 Core 指定順序逐項驗證，不能預設採收後自動重種。
- 個人任一存量耗至 0 或以下立即死亡；公共氧氣耗至 0 立即全員死亡，停止後续階段。不能等植物產氧再補救。
- 公共食物、水、電力歸零與植物死亡本身不直接結束。任一 crew 死亡即任務失敗；沒有成功通關、500 天期限或穩定計數。
- 玩家暫停、Core 規劃中與錯誤暫停時不推進世界，也不累計灌溉中斷。LLM 等待時間不能產生補跑 tick。
- 沒有預測 Simulator、Forecaster 或提供 Agent 試算未來的工具；實際結算函數可以供測試與重播使用。

嚴格結算順序：

```text
暫停／失敗檢查
→ 已安排進食與飲水
→ 個人基礎消耗及即時死亡檢查／公共呼吸及缺氧檢查
→ 未被其他工作占用的 crew 發電及死亡檢查
→ 製水及缺氧檢查
→ 作物灌溉、生長、產氧與灌溉中斷死亡
→ 依 Core 順序採收／清除／播種
→ 紀錄結算摘要、事件與狀態推播
```

新發的電可在本 tick 製水，新製的水可在本 tick 灌溉。本次 Phase 0 已核准 spec 的 pending 方案：作物產氧與收成預留容量，下一 tick 才由引擎入庫。這是相對 guide 入庫文字的明列偏差，詳見 docs/tick_order.md。每個消耗點即時檢查死亡；失敗狀態仍須推播。

## 5. 前端設計方向

以 v2 的 Gather Town 式正交俯視 2D 基地為基準，讓資源狀態直接呈現在設施上。不要改成以常駐儀表板或側欄為主的畫面。

- 地圖佔滿視窗，包含溫室、發電區、製水區、生命維持區、用餐區與生活區。
- 水箱液面、氧氣槽壓力環、電池亮格與食物堆層數反映後端庫存；hover／點擊顯示精確數值與單位。
- 呈現 20 塊地的作物種類、生長、成熟、灌溉中斷 1／2 tick 與死亡狀態。灌溉只有一個共用控制台，無比例滑桿。
- 四名 crew 顯示目前工作與移動動畫；點擊可看個人能量、水與存活狀態。位置插值與動畫不決定模擬耗時。
- 僅保留左上時間徽章、下方暫停／調速／修改資源工具列、右下 Agent 抽屜三處主要介面。
- Core／Plant／Human 氣泡分別錨定主控台、溫室與人員活動區；呈現觀察摘要、風險、建議、計畫、驗證錯誤與結果反思。
- 訊息呈現使用可供展示的決策摘要與結構化結果，不要求或依賴模型內部逐步思考內容。
- 任務失敗卡顯示原因與 tick，同時保留可見世界。低存量與死亡不可只靠顏色區分。
- 飲水點位置與 `clear` 前端入口仍待確認；必要的暫放位置需標為設計草案，不能當成已確認需求。

技術基準：React 18、TypeScript、Vite、PixiJS v8、Zustand；Tailwind 用於 overlay，Recharts 用於詳細卡。真正實作時先確認套件相容性與 lockfile，不因套件有新版就自行升級整套架構。

## 6. 後端與 Agent 串接

技術基準：Python 3.11、FastAPI、Pydantic v2、asyncio、SQLite。分離 API、排程／驗證、Agent adapter、世界結算、domain 與 persistence；底層不得反向依賴 API 或 LLM。

- 世界引擎以 `settle(state, plan) → (next_state, events)` 為可測試邊界；不要在數值結算中呼叫 LLM、播放動畫或寫 WebSocket。
- 與隊友共同定義 `WorldState`、`TickPlan`、`TickSummary`、`WorldEvent` 與 `AgentThought`。TypeScript 和 Pydantic 使用一致欄位、單位與驗證語意。
- `TickPlan` 必須表達目標 tick、crew 工作占用、補充對象／請求量／順序、發電量、製水請求、灌溉分配／順序、地塊操作與順序。
- 驗證未知 ID、作物 key、非有限數值、負請求、重複指派、無效工作量、過期計畫與非法狀態。錯誤需含可辨識的 code、target 和 message，供 Core 修正及 UI 顯示。
- 驗證與結算需考慮同 tick 狀態變化：發電後才有製水資源、作物可能在生長階段成熟或死亡、前項採收可使後項播種合法。不要只對 tick 起始 snapshot 做一次靜態判斷。
- 同一世界的 tick、資源干預與 reset 必須序列化。Core 規劃期間若狀態被玩家修改，舊計畫須失效並重新規劃，不能套用到不同版本世界。
- REST 與 WebSocket 控制共用相同後端服務。基準端點與訊息名稱見 v2 §7；連線中斷後應能重新取得權威 snapshot，避免重複或過期事件覆蓋新狀態。
- `/ingest/*` 若實作，不能成為 Agent 或前端任意覆寫庫存、跳過驗證與死亡檢查的入口。
- 連續設定與一次性動作分開：設定可依協定延續；補充、採收、播種與清除不得因沿用舊計畫再次執行。
- mock 計畫與固定策略只能用於前端展示、串接及回歸測試，不作正式世界引擎的隱藏 fallback。
- API key 留在後端環境變數；提供無秘密的 `.env.example`，不要寫入程式、前端 bundle 或日誌。

## 7. 已知文件差異與待確認事項

實作前注意以下差異，不要直接複製 v2 範例當成最終規格：

| 項目 | 處理原則 |
|---|---|
| PDF 的 500 天、40 m²、太陽能、營養細項、預測模擬與安全儲備硬限制 | 屬早期構想。現行範圍依 guide，不自動加入。 |
| v2 `pending_oxygen`／`pending_food` 延到下一 tick 入庫 | 本次已批准採 pending；容量使用庫存加 pending。玩家修改該項資源同時清除該項 pending，Core 與 UI 可見待入庫量。 |
| v2 `refills` 只有 crew 與 kind | 不能完整表達 guide 要求由 Core 指定的補充量。正式串接前補上請求量與明確順序語意，仍受單次上限限制。 |
| v2 驗證失敗三次後以空計畫結算 | 使用者已明定三輪無效計畫後空計畫結算並發 plan_failed；其他執行錯誤仍暫停。 |
| v2 在執行前 Reflect | 結果反思應依實際結算摘要；執行前可以提供計畫理由，不能宣稱已有執行結果。 |
| v2 氧氣溢出測例從 8,000 加 20 塊產氧 | 以目前係數不足以溢出 15,000 容量。測試應配置接近容量的庫存，不能照抄錯誤前提。 |
| v2 Demo 指定 AI 必須選某些作物或工作組合 | 作為展示示例；正式 AI 驗收觀察合法重規劃與結果，不強制固定策略。若需可重現畫面，使用標示清楚的 fixture。 |
| guide 提及 7,200 tick 通過 | 是文件記載的基準結果，不代表本 repository 已驗證；取得或建立相應測試後才能報告本地結果。 |

## 8. 開發方式與驗收

採小步完成可串接的垂直流程。首次建立專案時，建議順序是：共用模型與設定 → 世界結算及測試 → API／狀態推播 → 前端世界與控制 → Agent adapter／mock → 真實 Agent 整合。這是工作順序，不代表使用者每次要求都授權一次實作整個產品。

建議目錄為 `frontend/`、`backend/`、`shared/`、`checks/`；依實際任務逐步建立。已有程式後優先沿用專案慣例，不為符合示意架構做無關重寫。

修改世界模型時，至少驗證與變更相關的數值及邊界：

- 正常採收／發電／製水／灌溉、縮量、容量與產氧溢出。
- 個人補充不重複消耗、工作占用衝突與 Core 指定的執行順序。
- 缺水或缺電皆不得部分扣灌溉成本；連續三次失敗死亡，第二次後恢復保留進度。
- 呼吸、發電、製水、個人消耗或玩家修改造成的立即死亡，以及後續階段停止。
- 同 tick 採收後播種須為不同 crew 且有明確順序；新種作物下一 tick 生長。
- 暫停、Core 規劃等待、錯誤暫停、過期計畫與重連不造成額外 tick 或重複結算。

前端修改應確認世界物件和後端 snapshot 一致、角色動作對應實際結果、控制可用、斷線狀態可辨識，以及失敗原因能正確呈現。避免把外觀效果當成世界規則已完成的證據。

純文件或低風險外觀變更做相稱檢查即可，不必新增無關測試。建立應用骨架時，在 README 寫下實際可執行的安裝、啟動、mock 模式、型別檢查與測試命令；檔案尚不存在時，不編造命令或宣稱通過。

以繁體中文回報，程式識別字沿用英文 domain keys。完成時交代變更、驗證結果及尚待確認事項；保留使用者與隊友既有改動，不順手改寫原始設計文件。
